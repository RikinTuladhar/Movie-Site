import React from 'react'

const HomepageCover = ({children}) => {
  return (
    <div className='w-full relative bg-primary h-[auto]'>
        {children}
    </div>
  )
}

export default HomepageCover
